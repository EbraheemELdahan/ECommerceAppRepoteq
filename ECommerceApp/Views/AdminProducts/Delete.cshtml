@model ECommerceApp.Models.Product
@{
    ViewBag.Title = "Delete";
    ViewBag.Name = "Delete Product";
    Layout = "~/Views/Shared/_layoutAdmin.cshtml";

}


@using (Html.BeginForm("delete", "AdminProducts", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <h2> are you sure you want to delete this item</h2>
    @Html.Partial("Delete_Details")
    @*<div class="form-horizontal">
            <h2> are you sure you want to delete this item</h2>

            <dl>
                <dt>Product Image</dt>
                <dd><img src="~/Images/ProductsImages/@Model.ImageURL" style="border-radius:50%" height="70px" width="70px"></dd>
                <dt>Name</dt>
                <dd> @Model.Name</dd>
                <dt>Description</dt>
                <dd>@Model.Description</dd>
                <dt>Price</dt>
                <dd>@Model.Price</dd>
                <dt>Rate</dt>
                <dd>@Model.Rate</dd>
                <dt>Category</dt>
                <dd>@Model.Category.Name</dd>
                <dt>Brand</dt>
                <dd>@Model.Brand.Name</dd>
            </dl>
            <input type="submit" value="Delete" class="btn btn-danger" />
        </div>*@
}
