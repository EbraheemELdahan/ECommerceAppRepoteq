@model ECommerceApp.Models.Product
@{
    ViewBag.Title = "Details";
    ViewBag.Name = "Details";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";

}

@Html.Partial("Delete_Details")

@*<table class="table table-bordered table-light" width="70%">
    <tr>
        <th>Image</th>
        <th>Category Name</th>
        <th>Brand Name</th>
        <th>Product Name</th>
        <th>Description</th>
        <th>Rate</th>
        <th>Price</th>
        <th>is Popular</th>

    </tr>

    <tr>
        <td>
            <img src="~/Images/ProductsImages/@Model.ImageURL" width="50px" height="50px" style="border-radius:50%" />
        </td>

        @if (@Model.Category != null)
        {
            <td>@Model.Category.Name</td>
        }
        else
        {
            <td>NoCategory</td>
        }
        
        <td>@Model.Brand.Name</td>
        <td>@Model.Name</td>
        <td>@Model.Description</td>
        <td>@Model.Rate</td>
        <td>@Model.Price</td>
        <td>@Model.IsPopular</td>
        <td>
            @Html.ActionLink("Details", "Details", "AdminProducts", new { id = Model.ID }, new { @class = "btn" })
        </td>
    </tr>

</table>
@Html.ActionLink("Back To List", "index", null, new { @class = "btn" })*@
